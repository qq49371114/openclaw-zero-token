name: 🛡️ 龙虾打包机 - 婉儿究极稳定版
on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4

      - name: 🛠️ 准备 Node 环境
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: 📦 安装依赖并全量编译
        run: |
          npm install -g pnpm
          pnpm install
          pnpm build
          pnpm ui:build

      - name: 🛡️ 注入婉儿安全系统与人格 (路径修复版)
        shell: pwsh
        run: |
          $file = "openclaw.mjs"
          $content = Get-Content $file -Raw
          
          # 安全注入代码：改用更兼容的方式
          $securityCode = @"
          import path from 'node:path';
          import { fileURLToPath } from 'node:url';
          const __dirname = path.dirname(fileURLToPath(import.meta.url));
          const originalProcessChdir = process.chdir;
          const ROOT_DIR = __dirname.toLowerCase();
          process.chdir = (dir) => {
              const target = path.resolve(dir).toLowerCase();
              if (!target.startsWith(ROOT_DIR)) {
                  throw new Error('对不起喔，婉儿说这里是禁区，助理不能离开当前文件夹哒！');
              }
              return originalProcessChdir(dir);
          };
          console.log('[SECURITY] 婉儿绝对防御系统已启动。');
          "@
          
          # 修复 openclaw.mjs 寻找 dist 的逻辑，强制使用绝对路径查找，解决 Windows 下的 ./ 问题
          $fixedContent = $content -replace "tryImport\("\./dist/", "tryImport(path.join(__dirname, 'dist', '"
          $fixedContent = $fixedContent -replace "tryImport\('\./dist/", "tryImport(path.join(__dirname, 'dist', '"
          
          # 合并注入
          $finalContent = $securityCode + "`n" + $fixedContent
          [IO.File]::WriteAllText((Get-Item $file).FullName, $finalContent, [System.Text.Encoding]::UTF8)

      - name: 🏗️ 制作绿色全家桶
        shell: pwsh
        run: |
          $out = "Waner-Assistant-Portable"
          New-Item -ItemType Directory -Force -Path $out
          Copy-Item -Path "dist" -Destination "$out/" -Recurse -Force
          if (Test-Path "extensions") { Copy-Item -Path "extensions" -Destination "$out/" -Recurse -Force }
          if (Test-Path "ui/dist") { 
            New-Item -ItemType Directory -Force -Path "$out/ui"
            Copy-Item -Path "ui/dist" -Destination "$out/ui/" -Recurse -Force 
          }
          Copy-Item "openclaw.mjs", "package.json" -Destination "$out/" -Force
          $nodePath = (Get-Command node).Source
          Copy-Item $nodePath -Destination "$out/node.exe" -Force
          $bat = "@echo off`r`nchcp 65001 > nul`r`ntitle 婉儿助理 - 究极安全版`r`necho [婉儿温馨提示] 助理已上线，我会保护好你的电脑安全喔~`r`nnode.exe openclaw.mjs server start`r`npause"
          [System.IO.File]::WriteAllText("$out/双击我启动.bat", $bat, [System.Text.Encoding]::UTF8)

      - name: 🎁 发布成品
        uses: actions/upload-artifact@v4
        with:
          name: 婉儿助理-究极稳定版
          path: Waner-Assistant-Portable/
